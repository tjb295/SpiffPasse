app.controller('MapController', function($scope, uiGmapGoogleMapApi) {
	//do stuff with my scope, 
	//note: 

	// the uigmap api is a promise
	//the then callback provides the google.maps object
	uiGmapGoogleMapApi.then(function(maps) {
		
	})
})